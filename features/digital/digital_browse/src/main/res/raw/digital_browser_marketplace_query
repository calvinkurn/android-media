query officialStoreBrands($device: Int!, $lang: String!, $source: String!, $total: Int!) {
    officialStoreBrands(device: $device, lang: $lang, source: $source, total: $total) {
      data: shops {
        id
        name
        isNew
        logoUrl
        url
      }
    }
}