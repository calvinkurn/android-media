package com.tokopedia.digital_deals.view.utils;

import com.tokopedia.locationmanager.DeviceLocation;

public interface CurrentLocationCallBack {
    void setCurrentLocation(DeviceLocation deviceLocation);
}
