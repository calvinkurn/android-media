<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tokopedia.emoney">

    <uses-permission android:name="android.permission.NFC" />
    <!-- add library brizzi and force it to use minSdkVersion same as Tokopedia -->
    <uses-sdk tools:overrideLibrary="id.co.bri.sdk.brizzi"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name_emoney"
        android:supportsRtl="true">

        <activity
            android:name=".view.activity.EmoneyCheckBalanceNFCActivity"
            android:label="@string/emoney_toolbar_title_etoll_check_balance"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.White"
            android:windowSoftInputMode="stateAlwaysHidden">

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="@string/host_digital"
                    android:path="@string/path_emoney_brizzi"
                    android:scheme="@string/internal_scheme" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc_tech_filter" />

        </activity>

    </application>

</manifest>
