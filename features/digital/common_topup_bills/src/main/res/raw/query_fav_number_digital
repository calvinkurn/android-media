mutation favouriteNumber($categoryID : Int!){
  status
  recharge_favorite_number(categoryID: $categoryID) {
    list {
      client_number
      label
      product_id
      operator_id
      category_id
    }
  }
}