query hachikoCatalogQuery($device: Int) {
  mItems: slides(device: $device) {
    slides {
      id
      title
      imageUrl: image_url
      redirectUrl: redirect_url
      promoCode: promo_code
    }
  }
}
