<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <TextView
        android:id="@+id/txt_proceed"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/continue_form"
        android:textSize="@dimen/text_size_14"
        android:fontFamily="sans-serif-medium"
        android:textStyle="normal"
        android:padding="@dimen/padding_large_prof_comp"
        android:background="@drawable/profilecompletion_prof_comp_bg_textview_rounded"
        android:backgroundTint="@color/Unify_N100"
        android:textColor="@color/Unify_N200"
        />

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:gravity="center"
        />

    <TextView
        android:id="@+id/txt_skip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/skip_form"
        android:textSize="@dimen/text_size_14"
        android:fontFamily="sans-serif-medium"
        android:textStyle="normal"
        android:padding="@dimen/padding_large_prof_comp"
        android:visibility="gone"
        tools:visibility="visible"/>

</LinearLayout>
