mutation addPassword($password: String!, $password_confirm: String!, $hash: String!) {
  addPasswordV2(password: $password, password_confirm: $password_confirm, hash: $hash) {
    is_success
    error_code
    error_message
  }
}