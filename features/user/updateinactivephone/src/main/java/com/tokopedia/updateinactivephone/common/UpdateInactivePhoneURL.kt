package com.tokopedia.updateinactivephone.common

import com.tokopedia.url.TokopediaUrl

object UpdateInactivePhoneURL {
    var ACCOUNTS_DOMAIN = TokopediaUrl.getInstance().ACCOUNTS
    const val GET_UPLOAD_HOST = "apps/notactive/image/upload-host"
}
