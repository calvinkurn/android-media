mutation register($input: RegisterRequest!){
  register(input:$input){
    user_id
    sid
    access_token
    refresh_token
    token_type
    is_active
    action
    enable_2fa
    enable_skip_2fa
    errors {
      name
      message
    }
  }
}