query recent_view($userID: Int){
  get_recent_view(userID: $userID){
    items{
      product_id,
      product_name,
      shop_url,
      product_rating,
      product_review_count,
      product_image,
      shop_id
    }
  }
}