apply plugin: 'com.android.library'
apply from: "$rootProject.projectDir/buildconfig/kotlin/module.gradle"
apply plugin: 'io.hansel.preprocessor.module'

repositories {
    maven { url 'https://hansel.io/maven' }
}
buildscript {
    repositories {
        mavenCentral()
        maven { url 'https://hansel.io/maven' }
    }
    dependencies {
        classpath rootProject.ext.miscDependencies.hansel
    }
}
dependencies {
    implementation project(rootProject.ext.libraries.tkpddesign)

    implementation fileTree(dir: 'libs', include: ['*.jar'])
}

project.ext {
    artifactId = 'express-checkout-common'
    groupId = 'com.tokopedia.transaction'
    versionName = "1.0.0"
    artifactName = "transactionexpresscheckoutcommon-release"
}

apply from: '../../../publish_local.gradle'