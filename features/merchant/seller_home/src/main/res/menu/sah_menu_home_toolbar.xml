<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/sah_search_action_menu"
        android:title="@string/sah_search"
        app:actionLayout="@layout/sah_search_action_menu"
        app:showAsAction="always" />
    <item
        android:id="@+id/sah_notification_action_menu"
        android:title="@string/sah_notification"
        app:actionLayout="@layout/sah_notif_action_menu"
        app:showAsAction="ifRoom" />
</menu>