<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/progress">
        <clip>
            <shape android:shape="rectangle">
                <solid android:color="@color/Unify_GN400" /> <!-- Green progress color -->
                <corners android:radius="12dp" /> <!-- 12dp rounded corners, matching the background -->
            </shape>
        </clip>
    </item>
</layer-list>