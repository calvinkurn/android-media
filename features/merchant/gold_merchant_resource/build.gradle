apply plugin: 'com.android.library'
apply from: "$rootProject.projectDir/buildconfig/kotlin/module.gradle"

dependencies {

    testCompile rootProject.ext.testDependencies.junit
    androidTestCompile(rootProject.ext.testDependencies.espressoCore, {
        exclude group: 'com.android.support', module: 'support-annotations'
    })
}

project.ext {
    artifactId = 'gm-resource'
    groupId = 'com.tokopedia.merchant'
    versionName = "1.0.0"
    artifactName = "merchantgmresource-release"
}

apply from: '../../../publish_local.gradle'