query getCartRedirection($params:[CartRedirectionParams]){
  cart_redirection(params:$params){
    error_message,
    status,
    data {
      config_name
      available_buttons {
        text
        color
        cart_type
        onboarding_message
        show_recommendation
      }
      unavailable_buttons
    }
  }
}
