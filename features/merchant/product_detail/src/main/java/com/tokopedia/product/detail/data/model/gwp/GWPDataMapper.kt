package com.tokopedia.product.detail.data.model.gwp

/**
 * Created by yovi.putra on 02/08/23"
 * Project name: android-tokopedia-core
 **/
/*
fun GWPData.Data.Content.asUiModel(loadMoreText: String) = BMGMWidgetUiModel.Product(
    imageUrl = imageUrl,
    loadMoreText = loadMoreText
)

fun List<GWPData.Data.Content>.asUiModel(loadMoreText: String) = mapIndexed { index, content ->
    content.asUiModel(
        loadMoreText = if (index == size.dec()) {
            loadMoreText
        } else {
            ""
        }
    )
}

fun CustomAction.asGWPUiModel() = BMGMWidgetUiModel.Action(
    type = type,
    link = link
)

fun GWPData.Data.asUiModel(separator: String) =
    BMGMWidgetUiModel(
        title = title,
        iconUrl = iconUrl,
        products = contents.asUiModel(loadMoreText = loadMoreText),
        backgroundColor = backgroundColor,
        action = action.asUiModel(),
        titleColor = titleColor,
        separator = separator,
        offerId = offerId
    )*/
