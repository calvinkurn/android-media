<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppBarNoBottomShadow">
        <item name="elevation">0dp</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="TextDarkMode">
        <item name="android:textColor">@color/Unify_N700</item>
        <item name="android:textColorPrimary">@color/Unify_N700</item>
        <item name="android:textColorSecondary">@color/Unify_N500</item>
    </style>

</resources>
