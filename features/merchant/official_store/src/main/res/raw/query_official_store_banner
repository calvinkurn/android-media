query OfficialStoreBanners($slug: String) {
  slides {
		slides(page: $slug) {
          id
          title
          image_url
          topads_view_url
          redirect_url
          applink
        }
    }
}