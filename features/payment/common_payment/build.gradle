apply plugin: 'com.android.library'
apply from: "$rootProject.projectDir/buildconfig/module.gradle"
apply plugin: 'io.hansel.preprocessor.module'

repositories {
    maven { url 'https://hansel.io/maven' }
}
buildscript {
    repositories {
        mavenCentral()
        maven { url 'https://hansel.io/maven' }
    }
    dependencies {
        classpath rootProject.ext.miscDependencies.hansel
    }
}

project.ext {
    artifactId = 'common_payment'
    groupId = 'com.tokopedia.common.payment'
    versionName = "${tkpdPaymentVersion}"
    artifactName = 'commonpayment-release'
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

}

apply from: '../../../publish_local.gradle'
