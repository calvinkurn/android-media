mutation checkWhiteListStatus($updateStatus : Boolean!, $authValue : Int!, $token : String) {
   checkWhiteListStatus(updateStatus: $updateStatus, authValue: $authValue, token : $token) {
      status_code
      message
      data {
          user_email
          user_id
          state
          Token
      }
   }
}