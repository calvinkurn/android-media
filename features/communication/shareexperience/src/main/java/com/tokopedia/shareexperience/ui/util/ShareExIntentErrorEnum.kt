package com.tokopedia.shareexperience.ui.util

enum class ShareExIntentErrorEnum {
    AFFILIATE_ERROR,
    BRANCH_ERROR,
    DEFAULT_URL_ERROR, // Default url error means we are using default url because of some error(s)
    IMAGE_DOWNLOADER
}
