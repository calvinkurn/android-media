package com.tokopedia.tokofood.common.constants

import com.tokopedia.imageassets.ImageUrl

object LottieUrl {
    object OrderTracking {
        const val CHECK = ImageUrl.CHECK
        const val COOK = ImageUrl.COOK
        const val DELIVER = ImageUrl.DELIVER
        const val HOURGLASS = ImageUrl.HOURGLASS
    }
}