query DiscoPageLayoutQuery($identifier: String!, $version: String!, $device: String!) {
  discoveryPageInfo(identifier: $identifier, version: $version, device: $device) {
    data {
      title
      components
     page_info {
            Id
            Identifier
            Name
            Path
            Type
            share_config
            campaign_code
            search_applink
            search_title
            show_choose_address
          }
      additional_info
    }
  }
}