query paylater_getSimulation($amount: Int!){
	paylater_getSimulation(amount: $amount){
    gateways {
      gateway_id
      gateway_name
      simulation_detail {
        installment_per_month_ceil
        interest_pct
        is_recommended
        tenure
      }
    }
  }
}

