<?xml version="1.0" encoding="utf-8"?><!-- WHERE il == Instant Loan-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/il_main_content"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:background="@color/tkpd_main_green"
        android:gravity="top"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:text="@string/title_instant_loan"
            android:textColor="@color/white"
            android:textSize="@dimen/il_text_size_xlarge" />

    </android.support.v7.widget.Toolbar>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/container_outer_tab">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:animateLayoutChanges="true"
            android:orientation="vertical"
            android:paddingBottom="@dimen/il_padding_large">

            <include layout="@layout/layout_instant_loan_tabs" />

            <include layout="@layout/content_instant_loan_home_page" />

            <include layout="@layout/layout_bottom_banner" />

        </LinearLayout>
    </ScrollView>

</RelativeLayout>
