<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tokopedia.topads.auto">

    <application
        android:allowBackup="true"
        android:label="@string/app_name">
        <activity
            android:name=".activity.StartAutoAdsActivity"
            android:screenOrientation="portrait"
            android:label="@string/autoads_topads"
            android:theme="@style/Theme.White">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.DailyBudgetActivity"
            android:screenOrientation="portrait"
            android:label="@string/autoads_pengaturan_iklan"
            android:theme="@style/Theme.White" />
        <activity
            android:name=".activity.AutoAdsActivatedActivity"
            android:screenOrientation="portrait"
            android:label="@string/autoads_iklan_otomatis"
            android:theme="@style/Theme.White" />
    </application>

</manifest>