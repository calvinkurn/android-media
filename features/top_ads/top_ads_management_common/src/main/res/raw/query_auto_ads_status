query GetAutoAds($shopId: Int!){
  topAdsGetAutoAds(shopId:$shopId){
    data {
      shop_id
      status
      status_desc
      daily_budget
      daily_usage
      info {
        reason
        message
      }
    }
  }
}