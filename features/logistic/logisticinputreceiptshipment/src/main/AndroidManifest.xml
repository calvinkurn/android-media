<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tokopedia.logisticinputreceiptshipment">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity
            android:name=".view.barcodescanner.ReceiptShipmentBarcodeScannerActivity"
            android:label="@string/title_receipt_shipement_barcode_scanner"
            android:theme="@style/Theme.White" />
        <activity
            android:name=".view.confirmshipment.ConfirmShippingActivity"
            android:label="@string/title_confirm_shipment_logistic_module"
            android:theme="@style/Theme.White" />

    </application>
</manifest>
