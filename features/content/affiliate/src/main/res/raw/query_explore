query getExploreAffiliate($keyword: String, $nextCursor: String) {
  topadsExploreAffiliateProduct(keyword:$keyword, nextCursor:$nextCursor){
    products {
      adId
      productId
      image
      name
      commission
    }
    pagination{
      nextCursor
    }
  }
}