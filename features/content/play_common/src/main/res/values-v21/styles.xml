<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Play.FullScreen" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowContentOverlay">@null</item>

        <item name="android:ambientShadowAlpha">0</item>

        <!-- enable window content transitions -->
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>

        <item name="android:windowContentTransitions">true</item>

        <item name="android:windowSharedElementEnterTransition">@transition/anim_play_shared_enter</item>
        <item name="android:windowSharedElementReturnTransition">@transition/anim_play_shared_return</item>

    </style>

    <style name="PlayPlaceholder">
        <item name="android:background">@drawable/bg_play_placeholder</item>
        <item name="android:elevation">@dimen/play_placeholder_elevation</item>
    </style>

    <style name="PlayCirclePlaceholder">
        <item name="android:background">@drawable/bg_play_circle_placeholder</item>
        <item name="android:elevation">@dimen/play_placeholder_elevation</item>
    </style>
</resources>