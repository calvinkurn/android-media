<?xml version="1.0" encoding="utf-8"?>
<transitionSet
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:duration="@integer/animation_duration">

    <!-- Change bounds of all shared elements -->
    <transitionSet>
        <targets>
            <target android:targetId="@id/fl_fragment" />
            <target android:targetId="@+id/texture_view" />
        </targets>
        <changeBounds/>
        <changeImageTransform/>
        <changeTransform/>
    </transitionSet>

    <!-- Start fading background of activity -->
    <transition
        class="com.tokopedia.play.view.animation.FadeTransition"
        app:startAlpha="1.0"
        app:endAlpha="0.0">
        <targets>
            <target android:targetId="@id/fl_fragment" />
        </targets>
    </transition>

</transitionSet>