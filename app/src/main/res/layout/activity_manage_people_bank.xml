<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:context=".ManagePeopleBank" >

    <include
        android:id="@+id/include_loading"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        layout="@layout/loader"
        android:gravity="center"
        android:visibility="gone" />

    <include
        android:id="@+id/include_no_result"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        layout="@layout/error_no_result"
        android:visibility="gone" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/margin_small"
        android:orientation="vertical" >

        <!-- <TextView -->
        <!-- android:shopId="@+shopId/add_bank" -->
        <!-- android:layout_height="wrap_content" -->
        <!-- android:layout_width="match_parent" -->
        <!-- android:paddingTop="5dp" -->
        <!-- android:paddingBottom="5dp" -->
        <!-- android:gravity="center" -->
        <!-- android:text="Add bank address" -->
        <!-- android:background="@drawable/cards_ui_2_selected" -->
        <!-- android:layout_marginBottom="10dp" -->
        <!-- /> -->


        <!-- <TextView -->
        <!-- android:shopId="@+shopId/msg_no_bank" -->
        <!-- android:layout_height="wrap_content" -->
        <!-- android:layout_width="wrap_content" -->
        <!-- android:text="No bank" -->
        <!-- /> -->

        <ListView
            android:id="@+id/bank_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/margin_small"
            android:divider="@android:color/transparent"
            android:dividerHeight="@dimen/margin_small" />
    </LinearLayout>

</RelativeLayout>