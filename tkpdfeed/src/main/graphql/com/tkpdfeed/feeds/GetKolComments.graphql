query GetKolComments($idPost: Int!, $cursor: String, $limit: Int!) {
    get_kol_list_comment(idPost: $idPost, cursor: $cursor, limit: $limit) {
        error
        data {
            lastcursor
            total_data
            comment {
                id
                userID
                userName
                userPhoto
                comment
            }
        }
    }
}