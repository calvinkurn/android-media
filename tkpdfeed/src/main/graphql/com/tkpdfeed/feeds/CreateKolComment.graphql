mutation CreateKolComment($idPost: Int!, $comment: String){
	create_comment_kol(idPost: $idPost, comment: $comment) {
    		error
    		data {
    			id
    			user {
    				iskol
    				id
    				name
    				photo
    			}
    			comment
    			create_time
    		}
    	}
}