<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.SnackbarContentLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center_horizontal|bottom"
    android:layout_marginStart="8dp"
    android:layout_marginEnd="8dp"
    android:layout_marginBottom="24dp"
    android:background="@drawable/bg_toaster_red"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/snackbar_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|start"
        android:layout_marginLeft="@dimen/spacing_lvl4"
        android:layout_marginRight="@dimen/spacing_lvl4"
        android:layout_weight="1"
        android:ellipsize="end"
        android:maxLines="2"
        android:textAlignment="viewStart"
        android:textColor="@color/Neutral_N0"
        android:textStyle="bold"
        tools:text="Verifikasi berhasil, usia belum cukup untuk akses konten dewasa" />

    <Button
        android:id="@+id/snackbar_action"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|end"
        android:background="@null"
        android:textColor="@color/Neutral_N0"
        android:visibility="gone"
        tools:text="OK"
        tools:visibility="visible" />

</android.support.design.widget.SnackbarContentLayout>