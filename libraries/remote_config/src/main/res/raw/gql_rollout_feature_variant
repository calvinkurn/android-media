query RolloutFeatureVariants($rev: Int!, $client_id: Int!, $id: String){
     RolloutFeatureVariants(rev:$rev, client_id:$client_id, id:$id){
         featureVariants {
           feature
           variant
         }
         globalRev
         status
      }
 }