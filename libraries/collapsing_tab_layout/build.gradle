apply plugin: 'com.android.library'
apply from: "$rootProject.projectDir/buildconfig/module.gradle"

repositories {
}

buildscript {
    repositories {
        mavenCentral()
    }
}

dependencies {
    implementation project(rootProject.ext.libraries.abstraction)
    implementation project(rootProject.ext.libraries.design)
    implementation 'com.tokopedia.unify:principles:0.0.1-SNAPSHOT@aar'
    implementation rootProject.ext.supportLibDependencies.cardViewV7
}
