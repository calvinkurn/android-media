apply plugin: 'com.android.library'
apply from: "$rootProject.projectDir/buildconfig/module.gradle"

dependencies {
    compile project(rootProject.ext.libraries.network)
    compile project(rootProject.ext.libraries.useCase)
}
