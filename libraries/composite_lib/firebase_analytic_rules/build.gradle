apply plugin: 'java-library'

apply plugin: 'kotlin'

apply plugin: 'kotlin-kapt'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation rootProject.ext.supportLibDependencies.kotlinSupportJdk8
    compileOnly rootProject.ext.miscDependencies.javaPoet
    implementation rootProject.ext.tkpdInternalLibDependencies.analyticAnnotation
    implementation rootProject.ext.tkpdInternalLibDependencies.analyticParam
    implementation rootProject.ext.tkpdInternalLibDependencies.analyticProcessor
}

buildscript {

    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}
sourceSets {
    main {
        java {
            srcDirs = ['src/main/java', 'build/generated/source/kapt/main']
        }
    }
}
repositories {
    mavenCentral()
}
project.ext {
    artifactId = 'firebase'
    groupId = 'com.tokopedia.analytic.rules'
    versionName = "0.0.5"
    artifactName = "firebase_analytic_rules"
}

apply from: '../publish_local.gradle'
