mutation addShopNote($title:String!, $content:String!, $isTerms:Boolean!){
  addShopNote(input: {
      title: $title,
      content: $content,
      isTerms: $isTerms
    }) {
      success
      message
    }
}
