query SOMRejectReason($input:SOMRejectReasonRequest!) {
  som_reject_reason(input:$input) {
    reason_code
    reason_text
    reason_ticker
    child {
      reason_code
      reason_text
    }
  }
}