apply plugin: 'com.android.library'
apply from: "$rootProject.projectDir/buildconfig/kotlin/module.gradle"

dependencies {
    implementation rootProject.ext.playServicesDependencies.measurementApi

    lintChecks project(rootProject.ext.libraries.lint)
}

project.ext {
    artifactId = 'analytic_constant'
    groupId = 'com.tokopedia.analytic_constant'
    versionName = "1.0.3"
    artifactName = "analytic_constant-release"
}

apply from: '../publish_local.gradle'
