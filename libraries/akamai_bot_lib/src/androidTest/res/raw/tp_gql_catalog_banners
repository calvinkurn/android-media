query hachikoCatalogQuery($device: Int) {
  banners: slides(device: $device) {
    slides {
      id
      title
      imageUrl: image_url
      redirectUrl: applink
      promoCode: promo_code
    }
  }
}
