mutation UpdateShopNote($id:String!, $title:String!, $content:String!){
  updateShopNote(input: {
      id: $id,
      title: $title,
      content: $content
    }) {
      success
      message
    }
}
