{
  "readme": "Flight Search Tracking with Priority P1, separated with other priority level because currently we focused the instrument test with client analytics validator for tracking with priority P0 and P1",
  "mode": "exact",
  "query": [
    {
      "event": "viewSearchPage",
      "eventAction": "view search result flight",
      "eventCategory": "digital - flight",
      "eventLabel": "{{flight - .*-.* - .* - \\d*-\\d*-\\d* - .* - .*}}",
      "sessionIris": "{{.*}}",
      "travelWithKids": "{{true|false}}",
      "searchFound": "{{true|false}}",
      "destination": "{{.*}}",
      "deeplinkUrl": "{{.*}}",
      "returnDateFormatted": "{{.*}}",
      "url": "{{.*}}",
      "returnDate": "{{.*}}",
      "passenger": "{{\\d*}}",
      "returnTicket": "{{true|false}}",
      "from": "{{.*}}",
      "departureDate": "{{.*}}",
      "departureDateFormatted": "{{.*}}",
      "class": "{{.*}}"
    },
    {
      "event": "select_content",
      "eventAction": "product click",
      "eventCategory": "digital - flight",
      "eventLabel": "{{flight - .*-.*}}",
      "sessionIris": "{{.*}}",
      "item_list": "/flight",
      "items": [
        {
          "item_id": "{{.*}}",
          "index": "{{\\d*.\\d*}}",
          "item_name": "{{.*-.*}}",
          "dimension70": "{{true|false}}",
          "positions": "{{\\d*}}",
          "item_brand": "{{.*}}",
          "item_category": "Flight",
          "dimension76": "{{.*}}",
          "dimension75": "{{.*}}",
          "dimension67": "{{.*}}",
          "dimension66": "{{.*}}",
          "dimension72": "{{.*}}",
          "dimension71": "{{true|false}}",
          "dimension74": "{{.* - \\d*}}",
          "price": "{{\\d*.\\d*|\\d.\\d+E\\d}}",
          "dimension40": "/flight",
          "dimension73": "{{\\d* - \\d* - \\d*}}",
          "dimension69": "",
          "dimension68": "{{.*}}",
          "item_variant": "{{\\d* - \\d* - \\d*}}"
        }
      ]
    }
  ]
}