{
  "readme": "Shop page tracker",
  "mode": "exact",
  "query": [
    {
      "pageType": "/shoppage",
      "eventCategory": "{{shop page - (buyer|seller)}}",
      "shopId": "{{.+}}",
      "shopType": "{{.+}}",
      "event": "clickShopPage",
      "eventAction": "click sort",
      "sessionIris": "{{.+}}",
      "eventLabel":""
    },
    {
      "pageType": "/shoppage",
      "eventCategory": "{{shop page - (buyer|seller)}}",
      "shopId": "{{.+}}",
      "shopType": "{{.+}}",
      "event": "clickShopPage",
      "eventAction": "sort product",
      "sessionIris": "{{.+}}",
      "eventLabel":"{{.+}}"
    },
    {
      "pageType": "/shoppage",
      "eventCategory": "{{shop page - (buyer|seller)}}",
      "productId": "{{.+}}",
      "item_list": "{{/shoppage - (.+) - etalase chip (.+) - (login|non login) - search result}}",
      "dimension90": "{{.*}}",
      "shopId": "{{.+}}",
      "shopType": "{{.+}}",
      "event": "view_item_list",
      "eventAction": "{{product list impression - etalase chip (.+) - (login|non login) - search result}}",
      "sessionIris": "{{.+}}",
      "items": [{
        "item_id": "{{.+}}",
        "price": "{{^\\d+(.\\d+)*$}}",
        "dimension40": "{{/shoppage - (.+) - etalase chip (.+) - (login|non login) - search result}}",
        "dimension90": "{{.*}}",
        "index": "{{\\d*.\\d*}}",
        "item_name": "{{.+}}",
        "dimension81": "{{.+}}",
        "item_brand": "{{.+}}",
        "dimension79": "{{.+}}",
        "item_variant": "{{.+}}",
        "item_category": "{{.+}}"
      }],
      "eventLabel":"{{.+}}"
    },
    {
      "pageType": "/shoppage",
      "eventCategory": "{{shop page - (buyer|seller)}}",
      "productId": "{{.+}}",
      "item_list": "{{/shoppage - (.+) - etalase chip (.+) - (login|non login) - search result}}",
      "dimension90": "{{.*}}",
      "shopId": "{{.+}}",
      "shopType": "{{.+}}",
      "event": "select_content",
      "eventAction": "{{click product - etalase chip (.+) - (login|non login) - search result}}",
      "sessionIris": "{{.+}}",
      "items": [{
        "item_id": "{{.+}}",
        "price": "{{^\\d+(.\\d+)*$}}",
        "dimension40": "{{/shoppage - (.+) - etalase chip (.+) - (login|non login) - search result}}",
        "dimension90": "{{.*}}",
        "index": "{{\\d*.\\d*}}",
        "item_name": "{{.+}}",
        "dimension81": "{{.+}}",
        "item_brand": "{{.+}}",
        "dimension79": "{{.+}}",
        "item_variant": "{{.+}}",
        "item_category": "{{.+}}"
      }],
      "eventLabel":"{{.*}}"
    },
    {
      "pageType": "/shoppage",
      "eventCategory": "{{shop page - (buyer|seller)}}",
      "productId": "{{.+}}",
      "dimension90": "{{.*}}",
      "shopId": "{{.+}}",
      "shopType": "{{.+}}",
      "event": "clickShopPage",
      "eventAction": "{{(add|remove) wishlist - etalase chip (.+) - (login|non login) - search result}}",
      "sessionIris": "{{.+}}",
      "eventLabel":"{{.+}}"
    },
    {
      "pageType": "/shoppage",
      "eventCategory": "{{shop page - (buyer|seller)}}",
      "shopId": "{{.+}}",
      "shopType": "{{.+}}",
      "event": "clickShopPage",
      "eventAction": "click close filter",
      "sessionIris": "{{.+}}",
      "eventLabel":""
    },
    {
      "pageType": "/shoppage",
      "eventCategory": "{{shop page - (buyer|seller)}}",
      "shopId": "{{.+}}",
      "shopType": "{{.+}}",
      "event": "clickShopPage",
      "eventAction": "click etalase list",
      "sessionIris": "{{.+}}",
      "eventLabel":"{{etalase (.+)}}"
    },
    {
      "pageType": "/shoppage",
      "eventCategory": "{{shop page - (buyer|seller)}}",
      "shopId": "{{.+}}",
      "shopType": "{{.+}}",
      "event": "clickShopPage",
      "eventAction": "{{click etalase (.+)}}",
      "sessionIris": "{{.+}}",
      "eventLabel":""
    },
    {
      "pageType": "/shoppage",
      "eventCategory": "{{shop page - (buyer|seller)}}",
      "shopId": "{{.+}}",
      "shopType": "{{.+}}",
      "event": "clickShopPage",
      "eventAction": "click search page no result",
      "sessionIris": "{{.+}}",
      "eventLabel":"coba kata lain"
    },
    {
      "pageType": "/shoppage",
      "eventCategory": "{{shop page - (buyer|seller)}}",
      "shopId": "{{.+}}",
      "shopType": "{{.+}}",
      "event": "clickShopPage",
      "eventAction": "click search page no result",
      "sessionIris": "{{.+}}",
      "eventLabel":"search on tokopedia"
    },
    {
      "pageType": "/shoppage",
      "eventCategory": "{{shop page - (buyer|seller)}}",
      "productId": "{{.+}}",
      "item_list": "{{.+}}",
      "dimension90": "{{.*}}",
      "shopId": "{{.+}}",
      "shopType": "{{.+}}",
      "event": "view_item_list",
      "eventAction": "impression product search suggestion",
      "sessionIris": "{{.+}}",
      "items": [{
        "item_id": "{{.+}}",
        "price": "{{^\\d+(.\\d+)*$}}",
        "dimension40": "{{.+}}",
        "dimension90": "{{.*}}",
        "index": "{{\\d*.\\d*}}",
        "item_name": "{{.+}}",
        "dimension81": "{{.+}}",
        "item_brand": "{{.+}}",
        "dimension79": "{{.+}}",
        "item_variant": "{{.+}}",
        "item_category": "{{.+}}"
      }],
      "eventLabel":""
    },
    {
      "pageType": "/shoppage",
      "eventCategory": "{{shop page - (buyer|seller)}}",
      "productId": "{{.+}}",
      "item_list": "{{.+}}",
      "dimension90": "{{.*}}",
      "shopId": "{{.+}}",
      "shopType": "{{.+}}",
      "event": "select_content",
      "eventAction": "click product search suggestion",
      "sessionIris": "{{.+}}",
      "items": [{
        "item_id": "{{.+}}",
        "price": "{{^\\d+(.\\d+)*$}}",
        "dimension40": "{{.+}}",
        "dimension90": "{{.*}}",
        "index": "{{\\d*.\\d*}}",
        "item_name": "{{.+}}",
        "dimension81": "{{.+}}",
        "item_brand": "{{.+}}",
        "dimension79": "{{.+}}",
        "item_variant": "{{.+}}",
        "item_category": "{{.+}}"
      }],
      "eventLabel":""
    },
    {
      "pageType": "/shoppage",
      "eventCategory": "{{shop page - (buyer|seller)}}",
      "shopId": "{{.+}}",
      "shopType": "{{.+}}",
      "event": "clickShopPage",
      "eventAction": "click product list toggle",
      "sessionIris": "{{.+}}",
      "eventLabel":"{{.+}}"
    }
  ]
}