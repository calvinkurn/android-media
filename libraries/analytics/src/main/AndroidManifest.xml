<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tokopedia.analytics">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application>
        <activity
            android:name=".debugger.ui.activity.AnalyticsDebuggerActivity"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Green"
            android:taskAffinity="com.tokopedia.analytics.task"/>
        <activity
            android:name=".debugger.ui.activity.AnalyticsDebuggerDetailActivity"
            android:theme="@style/Theme.Green"/>
    </application>
</manifest>
