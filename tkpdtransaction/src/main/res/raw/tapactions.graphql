query tapactions($param: [TapActionsArgs]!) {
  orderDetailTapAction(input: $param) {
    label
    weight
    control
    header
    buttonType
    value
    method
    key
    name
    body {
     appURL
     method
     body
    }
  }
}