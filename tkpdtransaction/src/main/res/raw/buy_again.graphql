mutation add_to_cart_multi($param: [AddToCartMultiParam]) {
  add_to_cart_multi(param: $param) {
    error_message
    status
    data{
      success
      data {
        product_id
        quantity
        notes
        shop_id
      }
    }
  }
}