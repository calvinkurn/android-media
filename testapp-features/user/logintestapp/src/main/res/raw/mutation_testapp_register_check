mutation register_check ($id: String!){
    registerCheck(id: $id) {
        isExist
        isPending
        status
        registerType
        userID
        view
        uh
        errors
    }
}
