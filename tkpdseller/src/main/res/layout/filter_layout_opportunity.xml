<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/padding_small">

    <Spinner
        android:id="@+id/sort"
        android:layout_width="match_parent"
        android:layout_height="@dimen/btn_height"
        android:entries="@array/filter_sort_opportunity" />

    <Spinner
        android:id="@+id/shipping_type"
        android:layout_width="match_parent"
        android:layout_height="@dimen/btn_height"
        android:entries="@array/filter_supported_courier_opportunity" />

    <Spinner
        android:id="@+id/category"
        android:layout_width="match_parent"
        android:layout_height="@dimen/btn_height"
        android:entries="@array/filter_category_opportunity" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <Button
            android:id="@+id/button_confirm"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_marginTop="@dimen/margin_small"
            android:background="@color/teal_300"
            android:minHeight="32dp"
            android:text="@string/action_search" />

        <Button
            android:id="@+id/button_cancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginRight="@dimen/margin_small"
            android:layout_marginTop="@dimen/margin_small"
            android:layout_toLeftOf="@id/button_confirm"

            android:layout_toStartOf="@id/button_confirm"
            android:background="@color/grey_300"
            android:minHeight="32dp"
            android:text="@string/action_reset" />
    </RelativeLayout>

</LinearLayout>